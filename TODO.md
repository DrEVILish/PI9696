The reel-to-reel deck and settings modal have both been restyled into the
project's HUD theme (see PROJECT_STATUS "WebUI polish round"):

- The reel deck now sits inside the Transport Status panel on the project's
  panel-blue palette, and resizes proportionally to fit its column (the
  conflicting max-height that could distort the reels was removed - width and
  height now both derive from the intrinsic 820x265 aspect-ratio, capped by
  max-width).
- The settings modal is a grouped, responsive two-column sheet with a fixed
  header and scrollable body, with even vertical rhythm between sections.

